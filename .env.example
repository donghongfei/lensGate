# 复制此文件为 .env 并按需修改

# HTTP 监听端口（默认 3000）
# PORT=3000

# HTTP 绑定地址（默认 127.0.0.1，改为 0.0.0.0 允许局域网访问）
# HOST=127.0.0.1

# OAuth token 存储路径（默认 ~/.lensgate/agent/auth.json）
# 注意：.env 文件不展开 ~，请使用绝对路径，例如：
# AUTH_STORAGE_PATH=/home/pi/.lensgate/agent/auth.json

# 请求 Body 大小上限，字节（默认 4MB）
# BODY_LIMIT_BYTES=4194304

# CORS 允许来源（默认 *）
# CORS_ALLOW_ORIGIN=*

# 日志级别：debug | info | warn | error（默认 info）
# LOG_LEVEL=info

# 可选：限制访问代理的 API Key
# 设置后客户端须在 Authorization: Bearer <key> 中提供此值
# 不设置则不校验
# PROXY_API_KEY=
